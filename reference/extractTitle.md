# Extract Title

Extract title from list of title definitions. If set to use an
expression, convert to expression.

## Usage

``` r
extractTitle(titleList, default = "")
```

## Arguments

- titleList:

  (list) named list with title definitions, output of `plotTitlesServer`

- default:

  (character) default title if no title is set

## Value

(character) title
