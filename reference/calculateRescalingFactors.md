# Calculate Rescaling Factors

Calculate Rescaling Factors

## Usage

``` r
calculateRescalingFactors(oldLimits, newLimits = NULL)
```

## Arguments

- oldLimits:

  (numeric) old limits

- newLimits:

  (numeric) new limits

## Value

(list) with scale and center
