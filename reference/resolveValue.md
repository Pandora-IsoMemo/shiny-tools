# Resolve Value

Function to resolve the value, whether reactive or not

## Usage

``` r
resolveValue(var)
```

## Arguments

- var:

  The variable to resolve, can be reactive or not

## Value

The resolved value
